

export default function PageHeading({ className }) {
    return (
        <h2 className={`bg-gradient-to-r from-[#FF6A00] to-[#EE0979] inline-block text-transparent bg-clip-text uppercase text-4xl font-black ${className}`}>
            featured
        </h2>
    )
}