
function LoginListenBtn({ className, value }) {
    return (
        <button className={`w-[325px] h-[60px] rounded-full text-lg uppercase border-2 ${className}`}>{value}</button>
    )
}
export default LoginListenBtn;
